{
  "name": "memo-haus",
  "private": true,
  "version": "0.1.0",
  "description": "Scan a QR code, upload an old photo of Wolfsburg, and watch it become an interactive Gaussian-splat scene.",
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "setup:ml-sharp": "node scripts/setup-ml-sharp.mjs",
    "dev": "concurrently -n api,web,presentation -c cyan,green,magenta -k \"npm:dev:api\" \"npm:dev:web\" \"npm:dev:presentation\"",
    "dev:api": "node scripts/run-api.mjs",
    "dev:web": "npm --workspace @memo-haus/web run dev",
    "dev:presentation": "node scripts/serve-presentation.mjs",
    "build": "npm --workspace @memo-haus/web run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
